# Global config
RPC_URL=https://api.devnet.solana.com
X402_HEADER=X-Payment

# Server (packages/server/.env)
RECEIVER_PUBKEY=YOUR_RECEIVER_PUBKEY_HERE
PRICE_LAMPORTS=5000
STORE_DRIVER=file
STORE_FILE=./data/store.json
PORT=3000

# Optional: Real APIs (fallback to mocks if not set)
OPENAI_API_KEY=
OPENWEATHER_API_KEY=

# Agent (packages/agent/.env)
AGENT_MAX_LAMPORTS_PER_TASK=500000
AGENT_WHITELIST_HOSTS=localhost:3000
AGENT_MODEL=gpt-4o-mini

# Web (apps/web/.env)
NEXT_PUBLIC_API_BASE=http://localhost:3000
